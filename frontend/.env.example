# Where the browser reaches the Django API. Read at BUILD time (it is inlined
# into the JavaScript bundle), so changing it needs a rebuild.
#   ""                      same origin (production: CloudFront sends /api/* to Django)
#   http://localhost:8000   local development (Django runs on its own port)
NEXT_PUBLIC_API_URL=http://localhost:8000
